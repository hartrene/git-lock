This archive contains all files for the git-lock client.
These files have to be distributed to all git clients which like to participate.

bin: 
	All files in bin need to be in the PATH on each client.
hooks: 
	All files in the hooks directory have to be copied to .git/hooks in all participating git repositories.